// Create a string variable
let fullString = "JavaScript is awesome!";

// Extract a substring (e.g., "JavaScript")
let substring = fullString.substring(0, 10); // Extracts characters from index 0 to 9

// Log the substring to the console
console.log("Extracted Substring:", substring);

